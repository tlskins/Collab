<div class="container">
  <div class="text-center row vertical-center-row">
    <h2>Title <%= @source.title %></h2>
    <div class="text-center col-md-4 col-md-offset-4">
      <h5 class="text-center"><%= @post.created_at.strftime('%b %d, %Y') %></h5>
      <div><%= raw @post.body %></div>
    </div<
  </div<>
</div>

