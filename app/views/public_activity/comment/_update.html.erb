<li class="list-group-item">
  <span class="glyphicon glyphicon-edit"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.name : 'Guest' %></strong>
  <% if a.trackable %>
    updated a comment on Branch  <%= a.trackable.branch.name %> 
  <% else %>
    A comment that is currently deleted was edited.
  <% end %>
</li>
